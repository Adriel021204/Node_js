//Modules=function in js

const adriel = 'Adriel'
const arslaan = 'Arslaan'
const carlton = 'Carlton'

const sayHi =(name) => {               //sayHi funct in js
   console.log(`Hello there everyone!${name}`)
}

sayHi('Pooja')
sayHi(adriel)
sayHi(arslaan)
sayHi(carlton)

function sayHello(name) {
   console.log('Hello Everyone' + name);
}
  sayHello('Adriel')
  /*console.log(window) gives error,window or 
    document objects not used in js,used with 
    runtime envir,node js used with files,os*/
